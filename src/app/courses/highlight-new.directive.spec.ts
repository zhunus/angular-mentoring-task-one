import { HighlightNewDirective } from './highlight-new.directive';

describe('HighlightNewDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightNewDirective();
    expect(directive).toBeTruthy();
  });
});
